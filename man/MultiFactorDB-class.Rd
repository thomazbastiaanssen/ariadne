% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\name{MultiFactorDB}
\alias{MultiFactorDB}
\title{MultiFactor S7 container class}
\usage{
MultiFactorDB(x)
}
\value{
a \code{MultiFactor} object.
}
\description{
\code{MultiFactorDB} is an S7 class to interface, organize and manage multiple
sets of factors in a remote database. \code{MultiFactorDB} behaves like a regular.
\code{MultiFactor}. Methods for \code{MultiFactorDB} aim to follow \code{factor} behaviour.
}
\details{
The most straightforward way to construct a \code{MultiFactor} object is as a
named list of named data.frames. The columns of the data.frames indicate the
category of factor in that column.

A \code{MultiFactor} object presents itself similar to a \code{data.frame}, in the
sense that level types can be called as columns and individual data.frame
components can be called as rows.
\code{MultiFactor} inherits from \code{list}; Content can be accessed through regular
list methods (e.g., \code{[}, \code{[[}).
}
\section{Slots}{

\describe{
\item{\code{levels}}{\verb{Named list of character vectors}. Accessed through \code{levels(x)}}

\item{\code{value}}{Optional. \verb{Named list of vectors} of same size as \code{MultiFactor}
content, or \code{TRUE}, if missing.}

\item{\code{map}}{\verb{(sparse) Matrix} specifying which elements contain which levels.}
}}

